This, Bind, Call, and Apply in JavaScript
	Call invokes the function and allows you to pass in arguments one by one.
	Apply invokes the function and allows you to pass in arguments as an array.
	Bind returns a new function, allowing you to pass in a this array and any number of arguments
	D:\Personal\POC\Js\JS-Concepts\test\call-apply-bind.js
es6 features
	arrow fns vs regular fns see egs D:\Personal\POC\Js\JS-Concepts\test\arrowfn.js
variable Hoisting
var, let, const
	Keyword	Scope		Hoisting	Reassigned	Redeclared
	var		Function	Yes			Yes			Yes
	let		Block 		No			Yes			No
	const	Block 		No			No			No	
window
strict mode 3 methods
	detele a global variable not allowed
primitive datastype
	-String 
	-Boolean 
	-Number 
	-Undefined
	-Null 
	-Symbol 
map and set in es6
	Objects are used for storing keyed collections.
	Arrays are used for storing ordered collections.
	Map is a collection of keyed data items, just like an Object. But the main difference is that Map allows keys of any type.
	A Set is a special type collection – “set of values” (without keys), where each value may occur only once.

curry fns
closures
callback, promise, asycn await
Promise.allSettled() vs Promise.all()
shims vs polyfills
	Shims intercept API calls and create an abstract layer between the caller and the target. Typically shims are used for backward compatibility. 
	Polyfill is about implementing missing features in an API, whereas a shim wouldn’t necessarily be as much about implementing missing features as it is about correcting features
	So while shims are used for covering up old sins, polyfills are used for bringing future enhancements back in time



Inheritance and the prototype chain
prototype Inheritance vs Inheritance



// Let vs Var

for (var i = 0; i < 3; i++) {
  // or replace var with let, as it has block scope->output:0,1,2
  (i => {
    setTimeout(() => {
      console.log(i);
    }, 0);
  })(i);
}
console.log("after the loop");

//Closure
var outerAdd = function(outerParam) {
  var innerAdd = function(innerParam) {
    return outerParam + innerParam;
  };
  return innerAdd;
};

var add10 = new outerAdd(10);
var add20 = new outerAdd(20);

console.log("Add 10 : " + add10(5));
console.log("Add 20 : " + add20(5));

// CallBack vs Promise vs Async-Await
var posts = [
  { title: "Post One", body: "This is post one" },
  { title: "Post Two", body: "This is post two" }
];

function getPosts() {
  let body = "";
  setTimeout(() => {
    posts.forEach((post, index) => {
      body += `<li>${post.title}</li>`;
    });
    document.body.innerHTML = body;
  }, 500);
}

function failurePost() {
  alert("wrong format of post");
}

function addPost(post, successCallback, failureCallback) {
  setTimeout(() => {
    if (post.title && post.body) {
      posts.push(post);
      successCallback();
    } else {
      failureCallback();
    }
  }, 1500);
}

addPost(
  { title: "Post Three", newBody: "This is post three" },
  getPosts,
  failurePost
);


var posts = [
  { title: "Post One", body: "This is post one" },
  { title: "Post Two", body: "This is post two" }
];

function getPosts() {
  let body = "";
  setTimeout(() => {
    posts.forEach((post, index) => {
      body += `<li>${post.title}</li>`;
    });
    document.body.innerHTML = body;
  }, 500);
}

function failurePost(error) {
  alert(error);
}

function addPost(post) {
  return new Promise((resolve, reject) => {
    setTimeout(() => {
      posts.push(post);
      let error = false;
      if (!error) {
        resolve();
      } else {
        reject("Something went wrong");
      }
    }, 1500);
  });
}

function newAddPost(post) {
  setTimeout(() => {
    posts.push(post);
  }, 1500);
}

function updatePostsPromise() {
  addPost({ title: "Post Three", newBody: "This is post three" })
    .then(getPosts)
    .catch(error => failurePost(error));
}

async function updatePostsAsyncAwait() {
  try {
    await addPost({ title: "Post Three", body: "This is post three" });
    getPosts();
  } catch (error) {
    failurePost(error);
  }
}

updatePostsPromise(); //Promise
updatePostsAsyncAwait(); //Async-Await ->Latest implementation
redux concepts (three simple principles)
    single source of truth - The state of your whole application is stored in a single object
    State Is Read-Only - emit actions to change the state (type,payload, error (Any optional boolean property set to true if the action represents an error), 
        meta(Meta can be any type of value. It is intended for any extra information that is not part of the payload))
    State Is Changed with Pure Functions

Redux
-Is a predictable state container for JavaScript apps. (Actions,Reducer,Store)
-createStore(x) : x is the Reducer
-ACTION :	App dispatches actions to update the state with/without payloads.
			store.dispatch()
-REDUCER: 	Reducer returns a fresh instance of the state
			Specify how the application’s state changes in response to actions sent to the store
			Takes 2 arguments : State & Action
-STORE	:	Store notifies the app and re-render accordingly.
			Allows access to state via getState()
			Allows state to be updated via dispatch(action)
			Registers listeners via subscribe(listener)
-Alternative of Redux : MobX(new), RxJS (Reactive Extensions for JavaScript)
-Built in PubSub pattern

**Redux in React 4 steps**
	create a reducer
		const combineReducer = combineReducers({rA: reducerA,rB: reducerB});   //multiple reducers
	create a store
		const store = createStore(combineReducer, preloadedState,applyMiddleware(middleware));
	Wrap the app within the Provider from redux	
		<Provider store={store}><App /></Provider>
	Connect the compoent to redux using connect() HOC
		Container = connect(mapStateToProps, mapDispatchToProps)(Component);	

**Middleware**
const store = createStore(reducer, applyMiddleware(thunk));

Redux Thunk
-Action creators that return a function instead of an action
-To handle the asyncronous dispatching of actions(asyncronous api calls).
-Inner function receives the store methods 'dispatch' & 'getState' as parameters.
-applyMiddleware(thunk) is used

filter reducers
selectors
rendering user interface
dev tools
setting up hot reloading
testing redux
react router with redux
middleware
thunk vs saga
redux toolkit
immutable helpers